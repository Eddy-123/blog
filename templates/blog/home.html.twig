{% extends "base.html.twig" %}

{% block body %}
    {% for post in posts %}
        <p>{{ post.title }}</p>
    {% endfor %}
    
    <ul>
        {% for page in range %}
            <li>{{ page }}</li>
        {% endfor %}        
    </ul>
{% endblock %}
