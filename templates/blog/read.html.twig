{% extends "base.html.twig" %}

{% block body %}
    <h1 class="text-center">{{ post.title }}</h1>
    <p>{{ post.content }}</p>
    {% for comment in post.comments %}
        <p>{{ comment.content }}</p>
    {% endfor %}
{% endblock %}
